# MC Dashboard
Minecraft server status dashboard built with FastAPI, Jinja2, and mcstatus.
It tracks server availability, player counts, latency, version, MOTD, player samples, and recent history.

## Run
```bash
pip install -r requirements.txt
python app.py
```
Open http://127.0.0.1:18006 and add Minecraft Java server addresses from the web UI.
